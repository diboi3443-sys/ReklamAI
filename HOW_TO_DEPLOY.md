# 📦 Как загрузить файлы на сервер (reg.ru / ISPmanager)

## Что значит "Замените все файлы в public_html"?

Это означает, что нужно:
1. Распаковать архив `reklamai-deploy.zip`
2. Загрузить все файлы из архива на сервер
3. Поместить их в папку `public_html` (или корень сайта)

---

## 🚀 Пошаговая инструкция

### Вариант 1: Через File Manager (проще всего)

#### Для reg.ru:

1. **Войдите в панель управления reg.ru**
   - Откройте https://www.reg.ru/user/
   - Войдите в аккаунт

2. **Откройте File Manager**
   - Перейдите в **Хостинг** → выберите ваш домен
   - Нажмите **Файловый менеджер**

3. **Перейдите в папку public_html**
   - В файловом менеджере найдите папку `public_html`
   - Или `www`, или `htdocs` (зависит от хостинга)
   - Это корневая папка вашего сайта

4. **Удалите старые файлы (если есть)**
   - Выделите все файлы в `public_html`
   - Удалите их (кнопка "Удалить" или клавиша Delete)
   - ⚠️ **Важно:** Не удаляйте папку `public_html` саму, только файлы внутри!

5. **Загрузите новые файлы**
   - Распакуйте архив `reklamai-deploy.zip` на вашем компьютере
   - В файловом менеджере нажмите **Загрузить** или **Upload**
   - Выберите **все файлы** из распакованной папки:
     - `index.html`
     - `assets/` (вся папка)
     - `.htaccess`
     - `favicon.ico`
     - `robots.txt`
     - Все остальные файлы
   - Загрузите их в `public_html`

6. **Проверьте, что файлы загружены**
   - Убедитесь, что файл `.htaccess` загружен (он может быть скрыт)
   - Убедитесь, что папка `assets/` загружена полностью

#### Для ISPmanager:

1. **Войдите в панель ISPmanager**
   - Откройте панель управления вашего хостинга

2. **Откройте Файловый менеджер**
   - Найдите раздел **Файлы** или **File Manager**

3. **Перейдите в корень сайта**
   - Обычно это папка с названием вашего домена
   - Или `public_html`, `www`, `htdocs`

4. **Удалите старые файлы и загрузите новые**
   - То же самое, что для reg.ru (шаги 4-6 выше)

---

### Вариант 2: Через FTP (для опытных)

1. **Получите FTP данные**
   - В панели хостинга найдите **FTP доступ** или **FTP аккаунты**
   - Запишите:
     - **Хост:** `ftp.yourdomain.ru` или IP адрес
     - **Логин:** ваш FTP логин
     - **Пароль:** ваш FTP пароль

2. **Подключитесь через FTP клиент**
   - Используйте FileZilla, WinSCP, Cyberduck или другой FTP клиент
   - Введите данные подключения
   - Подключитесь к серверу

3. **Перейдите в public_html**
   - В FTP клиенте найдите папку `public_html`
   - Откройте её

4. **Удалите старые файлы**
   - Выделите все файлы в `public_html`
   - Удалите их

5. **Загрузите новые файлы**
   - Распакуйте архив `reklamai-deploy.zip` на вашем компьютере
   - Перетащите все файлы из распакованной папки в `public_html` через FTP клиент
   - Или используйте функцию "Загрузить" в FTP клиенте

6. **Проверьте права доступа**
   - Файлы: `644`
   - Папки: `755`
   - Обычно FTP клиент устанавливает права автоматически

---

## ✅ После загрузки

1. **Откройте ваш сайт в браузере**
   - Например: `https://yourdomain.ru`

2. **Очистите кэш браузера**
   - Нажмите `Ctrl+Shift+R` (Windows/Linux)
   - Или `Cmd+Shift+R` (Mac)
   - Это заставит браузер загрузить новые файлы

3. **Проверьте консоль браузера**
   - Нажмите `F12` → вкладка **Console**
   - Убедитесь, что нет ошибок
   - Проверьте, что загружается новый файл (например, `index-CCCVzR_9.js`)

4. **Создайте config.js (для reg.ru)**
   - Если используете reg.ru, создайте файл `config.js` в корне сайта
   - Скопируйте содержимое из `config.js.example`
   - Заполните ваши данные Supabase

---

## 📋 Чеклист

- [ ] Распакован архив `reklamai-deploy.zip`
- [ ] Открыт файловый менеджер или FTP клиент
- [ ] Перешел в папку `public_html` (или корень сайта)
- [ ] Удалены старые файлы
- [ ] Загружены все новые файлы из архива
- [ ] Проверено, что `.htaccess` загружен
- [ ] Проверено, что папка `assets/` загружена полностью
- [ ] Создан `config.js` (для reg.ru)
- [ ] Очищен кэш браузера
- [ ] Проверен сайт в браузере

---

## 🐛 Если что-то пошло не так

1. **Проверьте, что все файлы загружены**
   - Особенно `.htaccess` (может быть скрыт)
   - Папка `assets/` должна содержать файлы

2. **Проверьте права доступа**
   - Файлы: `644`
   - Папки: `755`

3. **Проверьте консоль браузера (F12)**
   - Посмотрите, какие ошибки там есть
   - Проверьте, загружаются ли файлы

4. **Проверьте config.js (для reg.ru)**
   - Убедитесь, что файл создан
   - Проверьте синтаксис JavaScript
   - Проверьте значения Supabase URL и ключа

---

## 📞 Нужна помощь?

Если возникли проблемы:
1. Проверьте логи сервера в панели хостинга
2. Проверьте консоль браузера (F12)
3. Убедитесь, что все файлы загружены правильно
