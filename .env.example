# ═══════════════════════════════════════════════════
# ReklamAI v2.0 — Environment Variables
# ═══════════════════════════════════════════════════

# ── Frontend (Vite) ──
VITE_API_URL=http://localhost:8000

# ── Backend (FastAPI) ──
DATABASE_URL=postgresql+asyncpg://reklamai_user:reklamai_password@db:5432/reklamai_db

# ── Auth (JWT) ──
JWT_SECRET=CHANGE-ME-generate-a-random-string
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=10080

# ── KIE.ai (Required for AI generation) ──
KIE_API_KEY=your-kie-api-key-here
KIE_BASE_URL=https://api.kie.ai

# ── Inngest (Background tasks) ──
INNGEST_EVENT_KEY=local
INNGEST_BASE_URL=http://inngest:8288

# ── Legacy Supabase (migration period, optional) ──
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key
