# Деплой на reg.ru - Подробная инструкция

## 📋 Подготовка

1. **Соберите проект:**
```bash
npm run build
```

2. **Проверьте папку `dist`:**
   - Должны быть файлы: `index.html`, `assets/`, `.htaccess`
   - Размер папки: ~1.3 MB

## 🚀 Варианты деплоя

### Вариант 1: Через File Manager (рекомендуется)

1. Войдите в [панель управления reg.ru](https://www.reg.ru/user/)
2. Перейдите в **Хостинг** → выберите ваш домен
3. Откройте **Файловый менеджер**
4. Перейдите в папку `public_html` (или `www`, `htdocs`)
5. **Удалите все старые файлы** (если есть)
6. Загрузите **все файлы** из папки `dist`:
   - `index.html`
   - `assets/` (вся папка)
   - `.htaccess`
   - `favicon.ico`
   - `robots.txt`
   - Все остальные файлы

7. **Важно:** Убедитесь, что файл `.htaccess` загружен (он может быть скрыт)

### Вариант 2: Через FTP

1. Получите FTP данные в панели reg.ru:
   - **Хост:** `ftp.yourdomain.ru` или IP адрес
   - **Логин:** ваш логин
   - **Пароль:** ваш пароль

2. Подключитесь через FTP клиент (FileZilla, WinSCP и т.д.)

3. Перейдите в `public_html` или `www`

4. Загрузите все файлы из `dist`

5. Установите права:
   - Файлы: `644`
   - Папки: `755`

## ⚙️ Настройка переменных окружения

reg.ru не поддерживает переменные окружения напрямую. Используйте `config.js`:

1. **Создайте файл `config.js`** в корне сайта (рядом с `index.html`):

```javascript
window.APP_CONFIG = {
  VITE_SUPABASE_URL: 'https://wgblrrhstqxwfiltkwcc.supabase.co',
  VITE_SUPABASE_ANON_KEY: 'ваш-anon-key-здесь'
};
```

2. **Замените значения** на ваши реальные из Supabase

3. **Загрузите `config.js`** на сервер в корень сайта

4. Файл `index.html` уже настроен для загрузки `config.js`

## 🔧 Настройка .htaccess

Файл `.htaccess` уже включен в `dist`. Он нужен для:
- SPA routing (чтобы все маршруты работали)
- Кэширования файлов
- Безопасности

**Проверьте:**
- Файл `.htaccess` загружен в корень сайта
- mod_rewrite включен в Apache (обычно включен по умолчанию)

## ✅ Проверка после деплоя

1. Откройте ваш сайт в браузере
2. Откройте консоль разработчика (F12)
3. Проверьте:
   - ✅ Нет ошибок в консоли
   - ✅ Файлы загружаются (Network tab)
   - ✅ Переменные окружения загружены (проверьте `window.APP_CONFIG`)
   - ✅ Роутинг работает (переход по страницам)

## 🐛 Решение проблем

### Черный экран
1. Откройте консоль (F12) → вкладка Console
2. Проверьте ошибки:
   - Если ошибка про `VITE_SUPABASE_URL` → создайте `config.js`
   - Если ошибка про файлы → проверьте пути к assets
   - Если другие ошибки → скопируйте и проверьте

### Ошибка 404 при переходе по страницам
- Проверьте, что `.htaccess` загружен
- Проверьте, что mod_rewrite включен (обратитесь в поддержку reg.ru)

### Файлы не загружаются (404)
- Проверьте права доступа: файлы `644`, папки `755`
- Проверьте, что папка `assets` загружена полностью
- Проверьте пути в консоли браузера

### Переменные окружения не работают
- Убедитесь, что `config.js` создан и загружен
- Проверьте синтаксис JavaScript в `config.js`
- Откройте консоль и проверьте `window.APP_CONFIG`

## 📞 Поддержка

Если проблемы остались:
1. Проверьте логи сервера в панели reg.ru
2. Проверьте консоль браузера (F12)
3. Обратитесь в поддержку reg.ru с описанием проблемы
