# Деплой на ISPmanager - Полная инструкция

## 📋 Подготовка проекта

1. **Соберите проект:**
```bash
npm install
npm run build
```

2. **Проверьте папку `dist`:**
   - Должны быть все файлы
   - Размер: ~1.3 MB

## 🖥️ Вариант 1: Статический сайт (Apache)

### Шаг 1: Загрузка файлов

1. В ISPmanager откройте **Файловый менеджер**
2. Перейдите в корневую директорию вашего сайта
3. Загрузите **все файлы** из папки `dist`:
   - `index.html`
   - `assets/` (вся папка)
   - `.htaccess`
   - Все остальные файлы

### Шаг 2: Настройка переменных окружения

ISPmanager поддерживает переменные окружения через `.env` файл:

1. Создайте файл `.env` в корне сайта:
```
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
```

2. **Или** используйте `config.js` (как для reg.ru):
```javascript
window.APP_CONFIG = {
  VITE_SUPABASE_URL: 'https://your-project.supabase.co',
  VITE_SUPABASE_ANON_KEY: 'your-anon-key-here'
};
```

### Шаг 3: Проверка .htaccess

1. Убедитесь, что `.htaccess` загружен
2. Проверьте, что mod_rewrite включен в Apache
3. Если нужно - включите в ISPmanager: **Настройки сайта** → **Apache модули** → включите `mod_rewrite`

## 🚀 Вариант 2: Node.js приложение

### Шаг 1: Загрузка проекта

1. Загрузите **весь проект** на сервер (не только `dist`)
2. Структура должна быть:
```
/path/to/project/
├── dist/
├── server.js
├── package.json
├── .env
└── ...
```

### Шаг 2: Настройка в ISPmanager

1. В ISPmanager создайте **Node.js приложение**:
   - **Путь к приложению:** `/path/to/your/project`
   - **Команда запуска:** `node server.js`
   - **Порт:** `3000` (или укажите свой)
   - **Рабочая директория:** `/path/to/your/project`

2. **Установите зависимости:**
```bash
cd /path/to/your/project
npm install --production
```

3. **Соберите проект:**
```bash
npm run build
```

### Шаг 3: Настройка переменных окружения

В ISPmanager:
1. Откройте **Node.js приложение** → **Переменные окружения**
2. Добавьте:
   - `VITE_SUPABASE_URL` = `https://your-project.supabase.co`
   - `VITE_SUPABASE_ANON_KEY` = `your-anon-key`
   - `PORT` = `3000` (опционально)

### Шаг 4: Запуск

1. В ISPmanager нажмите **Запустить** для Node.js приложения
2. Проверьте логи запуска
3. Откройте сайт в браузере

## ✅ Проверка после деплоя

1. Откройте сайт в браузере
2. Откройте консоль (F12)
3. Проверьте:
   - ✅ Нет ошибок
   - ✅ Файлы загружаются
   - ✅ Переменные окружения работают
   - ✅ Роутинг работает

## 🔧 Решение проблем

### Черный экран
- Проверьте консоль браузера
- Проверьте переменные окружения
- Проверьте логи сервера в ISPmanager

### Ошибки 404
- Проверьте `.htaccess` (для Apache)
- Проверьте настройки роутинга в Node.js

### Node.js не запускается
- Проверьте логи в ISPmanager
- Убедитесь, что порт свободен
- Проверьте права доступа к файлам

## 📝 Полезные команды

```bash
# Проверить статус Node.js приложения
pm2 status

# Посмотреть логи
pm2 logs

# Перезапустить
pm2 restart all
```
